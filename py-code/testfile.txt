Return a list of the lines in the string, breaking at line boundaries. 
“Split the string at the first occurrence of sep, and return a 3-tuple containing the part before the separator, the separator itself, and the part after the separator. If the separator is not found, return a 3-tuple containing the string itself, followed by two empty strings.”
Example 2: If a sep isn’t found in the string, a 3-tuple will return containing two empty strings, followed by the string itself
f a sep isn’t mentioned, it’ll raise a TypeError
en the text of all groups in the pattern are also returned as part of the resulting list. If maxsplit is nonzero, at most maxsplit splits occur, and the remainder of the string
tring with multiple delim